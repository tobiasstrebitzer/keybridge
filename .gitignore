node_modules/
dist/
*.log
.DS_Store
.env*

# dev-only: session notes / research docs
_docs/

# per-user local Claude Code settings (not shared)
settings.local.json

# local machine state lives under $HOME/.keybridge, never in-repo:
# credentials.json, config.json, keybridge-se-signer, keybridge-webshell

# npm artifacts (pnpm repo; the plugin launchers npm-install only inside the plugin cache)
package-lock.json

# roam-code local index
.roam/
.roamignore
